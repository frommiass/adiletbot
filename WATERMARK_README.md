# Добавление водяного знака в бота

## Что изменилось:

### Новые файлы:
1. **bot.py** - добавлен обработчик фото с водяным знаком
2. **config.py** - добавлена настройка `WATERMARK_TEXT`
3. **requirements.txt** - добавлена библиотека Pillow

## Быстрый старт:

### 1. Обнови зависимости:
```bash
pip install Pillow==10.4.0
```

Или установи все зависимости заново:
```bash
pip install -r requirements.txt
```

### 2. Настрой водяной знак в config.py:

Открой `config.py` и измени:
```python
WATERMARK_TEXT = "© Мой Водяной Знак"
```

На свой текст, например:
- `"© Ваше Имя"`
- `"© Не воруй моё фото"`
- `"@your_username"`

### 3. Перезапусти бота:

```bash
# Останови старого бота (Ctrl+C)
python bot.py
```

### 4. Как использовать:

**В личке с ботом или в группе:**

1. Отправь команду `/watermark` - получишь инструкцию
2. Отправь любое фото боту
3. Бот вернёт фото с водяным знаком!

## Как это работает:

- ✅ Водяной знак **повторяется** по всему изображению
- ✅ Текст **повёрнут** на 45 градусов по диагонали
- ✅ Полупрозрачный (сложно удалить)
- ✅ Работает с любыми фото

## Настройки (в bot.py):

Если хочешь изменить вид водяного знака, найди в `bot.py` функцию `handle_photo`:

```python
# Размер шрифта
font = ImageFont.truetype("arial.ttf", 50)  # ← измени 50

# Прозрачность (от 0 до 255)
fill=(255, 255, 255, 80)  # ← последнее число (80)

# Расстояние между повторениями
spacing_x = 350  # ← горизонталь
spacing_y = 200  # ← вертикаль

# Угол поворота
txt_layer_rotated = txt_layer.rotate(45, ...)  # ← измени 45
```

## Важно!

Бот теперь:
- ✅ Продолжает работать со всеми старыми функциями (саммари, статистика, реакции)
- ✅ **ПЛЮС** добавляет водяные знаки на фото

Все старые команды работают как раньше:
- `/summary` - саммари
- `/stats` - статистика
- `/reactions` - топ реакций
- `/app` - Mini App
- **НОВОЕ:** `/watermark` - инструкция по водяному знаку

## Что если не работает?

1. **Проверь установку Pillow:**
```bash
python -c "from PIL import Image; print('OK')"
```

2. **Проверь config.py:**
Убедись что есть строка:
```python
WATERMARK_TEXT = "© Твой текст"
```

3. **Проверь логи:**
Если бот выдаёт ошибку, она будет в консоли

## Полезно знать:

- Фото обрабатывается **локально** на сервере
- Временные файлы **автоматически удаляются**
- Качество JPG сохраняется (95%)
- Поддерживает любые разрешения

Всё! 🚀
