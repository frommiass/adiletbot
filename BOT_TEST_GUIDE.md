# Тестирование бота с водяным знаком

## Быстрый старт:

### 1. Установи зависимости (если ещё не установлены):
```bash
pip install Pillow
```

### 2. Запусти бота:
```bash
python bot.py
```

Должно появиться:
```
INFO:__main__:Restored 0 active chats
INFO:__main__:Bot started
```

### 3. Открой Telegram и найди своего бота

### 4. Протестируй:

**Шаг 1:** Отправь `/start`
Бот покажет список команд включая `/watermark`

**Шаг 2:** Отправь любое фото боту
Бот вернёт фото с водяным знаком "© ПРОКШИНО" под углом 30°

**Шаг 3:** Проверь результат
- ✅ Текст есть во всех углах
- ✅ Текст повёрнут на 30°
- ✅ Текст полупрозрачный (не слишком ярко)
- ✅ Размер фото не изменился

## Настройки в config.py:

Если хочешь изменить водяной знак, открой `config.py`:

```python
WATERMARK_TEXT = "© ПРОКШИНО"  # Твой текст
WATERMARK_ANGLE = 30  # Угол (попробуй 20, 30, 45)
WATERMARK_OPACITY = 60  # Прозрачность (40-100)
WATERMARK_SPACING_X = 300  # Плотность по горизонтали
WATERMARK_SPACING_Y = 150  # Плотность по вертикали
WATERMARK_FONT_SIZE = 36  # Размер шрифта
```

После изменения настроек:
1. Останови бота (Ctrl+C)
2. Запусти снова: `python bot.py`
3. Отправь новое фото

## Все функции бота работают!

✅ `/summary` - саммари за день
✅ `/stats` - статистика
✅ `/reactions` - топ по реакциям
✅ `/app` - Mini App
✅ **НОВОЕ:** Отправь фото → получишь с водяным знаком!

## Что тестировать:

1. **Разные размеры фото** - маленькие и большие
2. **Светлые и тёмные фото** - проверь видно ли текст
3. **Вертикальные и горизонтальные** фото
4. **В группе** - добавь бота в тестовую группу и отправь фото

## Если что-то не работает:

**Бот не отвечает:**
- Проверь токен в config.py
- Проверь что бот запущен

**Ошибка при обработке фото:**
- Посмотри логи в консоли
- Проверь что Pillow установлен: `pip list | grep Pillow`

**Текст не виден:**
- Увеличь `WATERMARK_OPACITY` (например до 100)
- Увеличь `WATERMARK_FONT_SIZE` (например до 48)

**Углы не покрыты:**
- Это уже исправлено! Но если всё равно проблема, уменьши `SPACING_X` и `SPACING_Y`

## Логи:

В консоли где запущен бот увидишь:
```
INFO:aiogram.event:Update id=... is handled
```

Если ошибка:
```
ERROR:__main__:Error adding watermark: ...
```

Готов к тестированию! 🚀
