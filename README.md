# Mini App для чата ПП5

Веб-приложение для Telegram Mini App с навигацией и функциями для жителей.

## 📁 Файлы

- `webapp.py` - FastAPI сервер
- `index.html` - Главная страница с меню
- `shahmatka.html` - Шахматка подъездов и квартир
- `statistics.html` - Статистика чата
- `contacts.html` - Контакты УК и служб

## 🚀 Установка

1. **Установите зависимости:**
```bash
pip install fastapi uvicorn aiosqlite --break-system-packages
```

2. **Скопируйте все файлы в одну папку:**
```bash
/opt/adiletbot/
├── webapp.py
├── index.html
├── shahmatka.html
├── statistics.html
├── contacts.html
└── messages.db (база данных бота)
```

3. **Запустите сервер:**
```bash
cd /opt/adiletbot
python3 webapp.py
```

Сервер запустится на `http://0.0.0.0:8080`

## 🔧 Настройка systemd (автозапуск)

Создайте файл `/etc/systemd/system/webapp.service`:

```ini
[Unit]
Description=Mini App WebServer
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/adiletbot
ExecStart=/usr/bin/python3 /opt/adiletbot/webapp.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Активируйте:
```bash
systemctl enable webapp
systemctl start webapp
systemctl status webapp
```

## 📱 Настройка в Telegram

1. Зайдите к @BotFather
2. Выберите своего бота
3. Нажмите "Menu Button" → "Edit Menu Button URL"
4. Укажите URL: `http://ваш-сервер:8080`

## ✨ Функции

### Главная страница
- 3 кнопки навигации:
  - 🏢 Шахматка
  - 📊 Статистика
  - 📞 Контакты

### Шахматка
- Сетка 4x4 подъездов (1-16)
- При клике → таблица 4x12 с квартирами
- 12 этажей, 4 квартиры на этаже

### Контакты
- Управляющая Компания А101: +7-495-648-67-77
- Бесплатная тележка: +7-909-166-56-03
- Кнопки быстрого звонка

### Статистика
- Общая статистика чата
- Топ активных участников
- Топ по реакциям
- Популярные эмодзи
- Фильтр: Сегодня / Неделя / Месяц

## 🎨 Дизайн

- Адаптивный дизайн для мобильных
- Поддержка темной/светлой темы Telegram
- Нативный вид Mini App
- Плавные анимации

## 🔄 Обновление контактов

Чтобы добавить новые контакты, отредактируйте `contacts.html`:

```html
<div class="contact-card">
    <div class="icon">🏢</div>
    <div class="contact-name">Название</div>
    <div class="contact-phone">
        <span class="phone-number">+7-XXX-XXX-XX-XX</span>
        <button class="call-button" onclick="callPhone('+7XXXXXXXXXX')">
            Позвонить
        </button>
    </div>
</div>
```

## 📝 Лог проверки

```bash
# Проверить работу сервера
curl http://localhost:8080

# Посмотреть логи
journalctl -u webapp -f
```

## ⚙️ Настройка шахматки

По умолчанию:
- 16 подъездов
- 12 этажей в каждом
- 4 квартиры на этаже
- Нумерация: (подъезд-1) × 48 + номер в подъезде

Чтобы изменить, отредактируйте `shahmatka.html`:
- Количество подъездов: строка `for (let i = 1; i <= 16; i++)`
- Количество этажей: строка `for (let floor = 12; floor >= 1; floor--)`
- Квартир на этаже: строка `for (let flatOnFloor = 1; flatOnFloor <= 4; flatOnFloor++)`
