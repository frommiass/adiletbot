# Модульная структура бота

## Файлы проекта:

```
📁 adiletbot/
├── bot.py                  ← Главный файл (запуск и регистрация handlers) 
├── photo_handler.py        ← ВСЁ про обработку фото
├── summary.py              ← ВСЁ про саммари и статистику
├── commands.py             ← Простые команды (/start, /reactions, текст)
├── miniapp.py              ← Mini App интеграция
├── watermark.py            ← Наложение водяных знаков (текст/PNG)
├── database.py             ← Работа с базой данных
├── llm.py                  ← GigaChat интеграция
├── config.py               ← Настройки
└── messages.db             ← База данных
```

---

## Что в каждом файле:

### 🤖 bot.py (60 строк)
**Главный файл - только запуск и регистрация**
- `main()` - инициализация БД, планировщика, регистрация handlers
- Больше ничего!

### 📸 photo_handler.py (~220 строк)
**Вся работа с фотографиями**
- `handle_group_photo()` - обработка фото в группах
- `handle_private_photo()` - обработка фото в личке
- `handle_media_group()` - логика сбора и обработки нескольких фото
- `process_single_photo()` - обработка одиночного фото
- `media_groups` - словарь для хранения медиа-групп
- `register_photo_handlers()` - регистрация handlers

**Что делает:**
- Скачивает фото из Telegram
- Применяет водяной знак (через watermark.py)
- Удаляет оригиналы (в группах)
- Постит обработанные фото
- Сохраняет в правильный топик

### 📊 summary.py (~100 строк)
**Вся работа с саммари**
- `cmd_summary()` - команда /summary
- `cmd_stats()` - команда /stats
- `generate_simple_summary()` - генерация саммари через GigaChat
- `send_daily_summary()` - отправка саммари по расписанию
- `active_chats` - список активных чатов
- `register_summary_handlers()` - регистрация handlers

**Что делает:**
- Собирает сообщения за день
- Генерирует умное саммари через GigaChat
- Считает статистику (активность, реакции)
- Отправляет в 23:15 автоматически

### ⌨️ commands.py (~120 строк)
**Простые команды и базовая логика**
- `cmd_start()` - команда /start
- `cmd_watermark()` - команда /watermark (инструкция)
- `cmd_reactions()` - команда /reactions (топы)
- `handle_group_text()` - сохранение текста в БД
- `handle_reaction()` - обработка реакций на сообщения
- `register_command_handlers()` - регистрация handlers

**Что делает:**
- Приветствие новых пользователей
- Показ инструкций
- Сохранение сообщений в БД
- Обработка реакций

### 📱 miniapp.py (~30 строк)
**Mini App интеграция**
- `cmd_app()` - команда /app
- `register_miniapp_handlers()` - регистрация handlers

**Что делает:**
- Показывает кнопку для открытия Mini App
- Передаёт chat_id в приложение

### 🎨 watermark.py (~150 строк)
**Наложение водяных знаков**
- `apply_watermark()` - выбор режима (text/png)
- `apply_text_watermark()` - текстовый водяной знак
- `apply_png_watermark()` - PNG логотип
- Автоматический fallback на текст если PNG не найден

**Что делает:**
- Накладывает текст или логотип
- По диагонали, повторяющийся
- Настраиваемая прозрачность
- Покрывает ВСЕ углы

### 💾 database.py (без изменений)
**Работа с SQLite**
- Сохранение сообщений
- Сохранение реакций
- Статистика

### 🧠 llm.py (без изменений)
**GigaChat API**
- Генерация умного саммари

### ⚙️ config.py (обновлён)
**Все настройки**
- Токены и API ключи
- Настройки водяного знака
- Время отправки саммари

---

## Преимущества новой структуры:

✅ **Модульность** - каждая фича в отдельном файле
✅ **Читаемость** - легко найти нужный код
✅ **Поддержка** - изменения не ломают другие части
✅ **Расширяемость** - легко добавлять новые фичи
✅ **Чистота** - bot.py всего 60 строк!

---

## Как обновить на сервере:

### 1. Загрузи все новые файлы:

```bash
cd ~/Downloads

scp bot_new.py photo_handler.py summary.py commands.py miniapp.py watermark.py config_updated.py root@89.23.117.38:/opt/adiletbot/
```

### 2. На сервере переименуй:

```bash
ssh root@89.23.117.38
cd /opt/adiletbot

# Бэкап старого бота
cp bot.py bot_old.py
cp config.py config_old.py

# Замени на новые
mv bot_new.py bot.py
mv config_updated.py config.py

# Проверь что все файлы на месте
ls -la
```

Должны быть:
- bot.py
- photo_handler.py
- summary.py
- commands.py
- miniapp.py
- watermark.py
- database.py
- llm.py
- config.py

### 3. Перезапусти:

```bash
systemctl restart adiletbot
systemctl status adiletbot
```

### 4. Проверь логи:

```bash
journalctl -u adiletbot -n 50
```

Должно быть:
```
INFO:__main__:Restored X active chats
INFO:__main__:Bot started
```

---

## Тестирование:

После обновления протестируй все функции:

1. **Команды:**
   - `/start` - должен показать список команд
   - `/stats` - статистика за день
   - `/summary` - саммари за день
   - `/reactions` - топы по реакциям
   - `/watermark` - инструкция

2. **Фото в личке:**
   - Отправь фото боту в личку
   - Должен вернуть с водяным знаком

3. **Фото в группе (одиночное):**
   - Отправь 1 фото в группу
   - Должен удалить оригинал
   - Запостить с водяным знаком
   - С подписью "📸 Фото от @username"

4. **Фото в группе (несколько):**
   - Отправь 3-5 фото разом
   - Должен удалить все оригиналы
   - Запостить все с водяными знаками одним сообщением

5. **Топики:**
   - Отправь фото в разные топики
   - Должен постить в тот же топик откуда получил

6. **Автоматическое саммари:**
   - Дождись 23:15 (или измени время в config.py)
   - Должен автоматически отправить саммари

---

## Структура handlers (для разработчиков):

```python
# bot.py регистрирует handlers из каждого модуля:

commands.register_command_handlers(dp)
# → /start, /watermark, /reactions
# → Обработка текста в группах
# → Обработка реакций

summary.register_summary_handlers(dp)
# → /summary, /stats

photo_handler.register_photo_handlers(dp, bot)
# → Фото в группах
# → Фото в личке
# → Медиа-группы

miniapp.register_miniapp_handlers(dp)
# → /app
```

---

## Что дальше?

Теперь легко добавлять новые фичи:

**Хочешь добавить игры?**
→ Создай `games.py` с функциями и `register_game_handlers()`
→ В `bot.py` добавь `games.register_game_handlers(dp)`

**Хочешь добавить опросы?**
→ Создай `polls.py` с логикой опросов
→ Зарегистрируй handlers в `bot.py`

**Хочешь изменить логику фото?**
→ Открой `photo_handler.py` - всё там!

---

## Troubleshooting:

**Бот не запускается:**
```bash
# Проверь синтаксис
python3 -m py_compile bot.py
python3 -m py_compile photo_handler.py
python3 -m py_compile summary.py
python3 -m py_compile commands.py
python3 -m py_compile miniapp.py
```

**ModuleNotFoundError:**
```bash
# Убедись что все файлы в одной папке
ls -la /opt/adiletbot/
```

**Фото не обрабатываются:**
```bash
# Проверь логи
journalctl -u adiletbot -f

# Проверь что watermark.py на месте
ls -la /opt/adiletbot/watermark.py
```

---

Структура готова! Всё чисто, модульно и легко расширять! 🚀
