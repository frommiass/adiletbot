# Как запустить Mini App для бота

## Что добавилось:

1. **webapp.py** - веб-сервер с API для статистики
2. **index_miniapp.html** - интерфейс Mini App  
3. **bot.py** - добавлена команда `/app`
4. **requirements.txt** - добавлены fastapi и uvicorn

## Быстрый старт:

### 1. Обнови зависимости:
```bash
pip install -r requirements.txt
```

### 2. Настрой URL в bot.py:

Открой `bot.py`, найди строку:
```python
webapp_url = "https://your-domain.com"
```

Замени на:
- Если есть домен: `"https://your-domain.com"`
- Если IP сервера: `"http://123.45.67.89:8080"` (замени на свой IP)

### 3. Запусти веб-сервер:

В отдельном терминале:
```bash
python webapp.py
```

Это запустит API на порту 8080.

### 4. Перезапусти бота:

```bash
# Останови старого бота (Ctrl+C)
python bot.py
```

### 5. Открой в Telegram:

В чате с ботом напиши:
```
/app
```

Нажми кнопку "📊 Открыть статистику" - откроется Mini App!

## Если у тебя уже настроен nginx:

Добавь в конфиг:
```nginx
location / {
    proxy_pass http://127.0.0.1:8080;
}
```

Тогда в bot.py используй свой домен с HTTPS.

## Запуск в фоне (если нужно):

```bash
# Запустить веб-сервер в фоне
nohup python webapp.py > webapp.log 2>&1 &

# Запустить бота в фоне  
nohup python bot.py > bot.log 2>&1 &
```

## Что делает Mini App:

- Показывает статистику за день/неделю/месяц
- Топ активных пользователей
- Топ по реакциям
- Популярные эмодзи
- Обновление данных в реальном времени

Всё! 🚀
